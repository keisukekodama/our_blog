.postTitle
  = @user.nickname
  さんの投稿一覧
-# DBからとって来た文字列と文字をつなげて表示させてい場合は横ではなく一つしたの行にかきこむ
- @posts.each do |post|
  -# = render partial: "posts/post", locals: { post: post }
  = render post


-# hamlに変にコメントアウト入れるとインデントのエラーがでる
